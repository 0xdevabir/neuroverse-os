# tests/meson.build — test runner registration (placeholder)
# Real tests are added per subsystem as they're implemented (Phase A onwards).

# No tests yet. Each future commit under tests/ adds a test executable here.